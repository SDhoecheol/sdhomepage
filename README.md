# Adsland Lite

A minimal example website inspired by [adsland.com](https://www.adsland.com/). This project uses Node.js and Express to serve a few static pages.

## Development

Install dependencies and start the server:

```bash
npm install
npm start
```

Then open `http://localhost:3000` in your browser.
